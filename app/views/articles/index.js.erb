$('tbody#articles').html('<%= escape_javascript render(partial: "article", :collection => @articles, :as => :article) %>');

$('#paginator').html('<%= escape_javascript(paginate(@articles, :remote => true).to_s) %>');
