<tr>
  <td><%= image_tag(article.photo_small) if article.photo_small? %> </td>
  <td>
    <p id="article_title"><%= link_to article.title, blog_article_path(article) %></p>
    <p><%= truncate(strip_tags(article.text), length: 1000) %></p>
    <p>by <%= article.user.name %></p>
  </td>
</tr>
