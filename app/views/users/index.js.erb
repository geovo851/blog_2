$('tbody#users').html('<%= escape_javascript render(partial: "user", :collection => @users, :as => :user) %>');

$('#paginator').html('<%= escape_javascript(paginate(@users, :remote => true).to_s) %>');
